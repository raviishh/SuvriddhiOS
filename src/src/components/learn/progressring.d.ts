interface ProgressRingProps {
    size?: number;
    stroke?: number;
    progress: number;
}
export default function ProgressRing({ size, stroke, progress }: ProgressRingProps): import("preact").JSX.Element;
export {};
